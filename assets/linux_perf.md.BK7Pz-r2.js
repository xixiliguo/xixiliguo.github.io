import{_ as e,c as a,o as r,a2 as t}from"./chunks/framework.ry9uveZm.js";const f=JSON.parse('{"title":"性能优化指南","description":"","frontmatter":{},"headers":[],"relativePath":"linux/perf.md","filePath":"linux/perf.md","lastUpdated":1789138315000}'),o={name:"linux/perf.md"},l=t(`<h1 id="性能优化指南" tabindex="-1">性能优化指南 <a class="header-anchor" href="#性能优化指南" aria-label="Permalink to &quot;性能优化指南&quot;">​</a></h1><p>提供性能问题的分析思路和常见的原因。</p><h2 id="前言" tabindex="-1">前言 <a class="header-anchor" href="#前言" aria-label="Permalink to &quot;前言&quot;">​</a></h2><p>性能类问题首先要找到差异点，即对比基准测试（正常情况），观察哪些性能指标发生了巨大变化。</p><p>收集尽可能多的环境信息，缩小范围：</p><ul><li>虚拟机还是物理机，具体的规格。</li><li>OS版本（特别是内核版本）。</li><li>k8s环境，网络和存储模型，POD的resource(cpu,memory)配置，容器镜像版本。</li><li>中间件版本（比如jre, 与mysql,redis交互使用的库）。</li><li>业务软件版本，配置文件。</li><li>压测时客户端的输入参数，并发数， tps等。</li></ul><p>性能瓶颈有两种：</p><ol><li>util 使用率，在资源有限的情况下使用程度接近limit, 比如cpu 100%。</li><li>latency 延迟， 整体响应=请求等待+请求处理时间。请求等待是非必需的，尽量缩短。</li></ol><h2 id="虚拟化" tabindex="-1">虚拟化 <a class="header-anchor" href="#虚拟化" aria-label="Permalink to &quot;虚拟化&quot;">​</a></h2><p>常见的性能下降原因：</p><ol><li>降频</li><li>超分</li><li>范围绑核</li><li>内存带宽</li></ol><p>kvm需要观察vmexit的次数。</p><h3 id="降频" tabindex="-1">降频 <a class="header-anchor" href="#降频" aria-label="Permalink to &quot;降频&quot;">​</a></h3><p>负载不高时，CPU频率可达到睿频，但当整体功耗超过一定值就会降到基频。 降频发生的场景：</p><ol><li>宿主机vpu对应的物理cpu socket整体CPU使用率高，接近满载。</li><li>虚拟机里自身应用使用向量指令（比如intel的avx512）</li></ol><p>场景1是整体物理核负载高。<br> 场景2是自身原因导致，avx512这种向量指令需要更多的power<br> 一旦降频，整个物理核下面运行的虚拟机都会受到影响，叫做扰邻。</p><p>参考：<br><a href="https://serverfault.com/questions/1064327/can-intel-turbo-boost-frequency-for-all-cores-be-lower-than-the-base-frequency" target="_blank" rel="noreferrer">https://serverfault.com/questions/1064327/can-intel-turbo-boost-frequency-for-all-cores-be-lower-than-the-base-frequency</a><br><a href="https://blog.cloudflare.com/on-the-dangers-of-intels-frequency-scaling/" target="_blank" rel="noreferrer">https://blog.cloudflare.com/on-the-dangers-of-intels-frequency-scaling/</a></p><p>在支持pmu的环境下，可执行<code>perf stat yes&gt;/dev/null</code>(执行5s后按ctrl+c停止)，能实测当前的cpu频率。</p><p>内存带宽， intel 提供rdt来支持统计。</p><p>关闭超线程能提升单cpu处理能力。</p><p>虚拟化场景开启mwait可以让mwait指令不陷入到vm-exit，减少latency。</p><h2 id="cpu-内存" tabindex="-1">CPU/内存 <a class="header-anchor" href="#cpu-内存" aria-label="Permalink to &quot;CPU/内存&quot;">​</a></h2><p>usr 说明cpu耗在用户态程序上面的百分比。<br> sys 说明cpu耗在内核态上面的百分比。</p><p>load average 负载，是估算值。 load 约等于 处于R状态的进程数+ 处于D状态的进程数。因为负载的采集周期是5s，所以性能监控软件的采样频率要小于5s，这样每个进程的状态能观察的更准确些。</p><p>user,sys, hi, si 等cpu持续高，可直接使用perf采样，分析热点函数。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>perf record -ag sleep 10</span></span>
<span class="line"><span>perf report</span></span></code></pre></div><p>iowait 解释:<br> iowait% 计算的是在cpu空闲状态时，同时有io等待。<br> 但是 io等待可以发生在空闲状态， 也可以发生在忙的状态。</p><p>内存紧张时会触发回收，如果是直接回收会直接阻塞当前的进程。需要重点关注<code>/proc/vmstat</code>里的pgscan，pgsteal等指标。</p><p>高内核在amd CPU上面可能出现性能下降的情况，需要关闭安全相关的内核参数。</p><p>如果只有某个core或者某个numa高，user和sys都高，可能是硬件的原因。</p><h2 id="网络" tabindex="-1">网络 <a class="header-anchor" href="#网络" aria-label="Permalink to &quot;网络&quot;">​</a></h2><p>单纯的网络测速，可使用<code>iperf</code>或者<code>wget xxxx &gt;/dev/null</code>。 这样避免了落盘的干扰。</p><h3 id="网卡多队列" tabindex="-1">网卡多队列 <a class="header-anchor" href="#网卡多队列" aria-label="Permalink to &quot;网卡多队列&quot;">​</a></h3><p>多队列可以极大地提升网络性能，特别是发现单核cpu si非常高，且对应的ksoftirq进程cpu很高的情况。<br> OS层面可以继续打开RPS，XPS等特性，进一步提升性能。<a href="https://docs.kernel.org/networking/scaling.html%EF%BC%8C" target="_blank" rel="noreferrer">https://docs.kernel.org/networking/scaling.html，</a> 但如果网卡队列数&gt;=CPU数目，则没必要使用RPS。</p><p>tcp buffer 要大于等于 带宽时延积， 带宽的单位是 Gb/s, 时延可使用ping获取。</p><p>网卡rx方向慢考虑两方面：</p><ol><li>单核cpu使用率近乎100%， 影响该cpu上面ksoftirq的调度。</li><li>slab内存多， 可运行<code>echo 2 &gt;/proc/sys/vm/drop_cache</code>后观察</li></ol><p>如何分析wireshark里的单个TCP连接的发送性能，利用tcptrace。<br><a href="https://www.packetsafari.com/blog/2021/10/31/wireshark-tcp-graphs" target="_blank" rel="noreferrer">https://www.packetsafari.com/blog/2021/10/31/wireshark-tcp-graphs</a></p><h3 id="常见的丢包点" tabindex="-1">常见的丢包点 <a class="header-anchor" href="#常见的丢包点" aria-label="Permalink to &quot;常见的丢包点&quot;">​</a></h3><p>ip -s -s link 查看网卡层面是否有 error,drop,missed等</p><p>/proc/net/softnet_stat 查看软中断是否丢包， 第二列表示软中断的丢包数</p><p>/proc/net/stat/arp_cache 里倒数第一列，第二列， 如果有值，代表有丢包发生。</p><p>tc -s qdisc show dev eth0 关注 dropped 字段是否非零</p><h3 id="tcp重传" tabindex="-1">TCP重传 <a class="header-anchor" href="#tcp重传" aria-label="Permalink to &quot;TCP重传&quot;">​</a></h3><p>重传发生时，说明网络有丢包/乱序</p><p>nstat -r -z 可查询大量关于网络指标的计数</p><p>sys重传 TcpExtTCPSynRetrans 增加， TcpRetransSegs 增加<br> sys-ack重传 TcpExtTCPSynRetrans 增加， TcpRetransSegs 增加。只能通过kprobe， tracepoint tcp_retransmit_synack 跟踪来确定到底是syn还是syn-ack重传<br> RTO重传，没有准确的计数<br> TcpRetransSegs == TcpExtTCPSlowStartRetrans + TcpExtTCPFastRetrans + TcpExtTCPSynRetrans + RTO重传的包 + 部分TcpExtTCPLossProbes</p><p>参考：<a href="https://arthurchiao.art/blog/tcp-retransmission-may-be-misleading" target="_blank" rel="noreferrer">https://arthurchiao.art/blog/tcp-retransmission-may-be-misleading</a></p><h3 id="tcpinerr" tabindex="-1">TcpInErr <a class="header-anchor" href="#tcpinerr" aria-label="Permalink to &quot;TcpInErr&quot;">​</a></h3><p>该计数增长，意味着rx方向有丢包，分三种情况：</p><ol><li>checksum校验不通过，可通过<code>TcpInCsumErrors</code>确认。</li><li>已经处于<code>ESTAB</code>状态的TCP连接，再次收到syn报文， 可通过 <code>TcpExtTCPSYNChallenge</code> 确认。</li><li>数据实际<code>size</code>小于TCP包头。</li></ol><h3 id="内核参数" tabindex="-1">内核参数 <a class="header-anchor" href="#内核参数" aria-label="Permalink to &quot;内核参数&quot;">​</a></h3><p>net.ipv4.tcp_slow_start_after_idle配置为0，可使空闲连接再次发包时不再慢启动。</p><p>使用bbr算法时，必须调整tc调度，配置为fq_codel</p><p>net.ipv4.tcp_mem 控制整个系统所有TCP buffer，单位是页。 如果连接特别多，可能达到压力阈值而降速。/proc/net/sockstat 可查实际值</p><h2 id="磁盘io" tabindex="-1">磁盘IO <a class="header-anchor" href="#磁盘io" aria-label="Permalink to &quot;磁盘IO&quot;">​</a></h2><h3 id="磁盘调度" tabindex="-1">磁盘调度 <a class="header-anchor" href="#磁盘调度" aria-label="Permalink to &quot;磁盘调度&quot;">​</a></h3><p>/sys/block/vda/queue/scheduler 记录当前生效的调度算法。</p><h3 id="多队列" tabindex="-1">多队列 <a class="header-anchor" href="#多队列" aria-label="Permalink to &quot;多队列&quot;">​</a></h3><p>/sys/block/vda/mq下面只有一个文件夹，名字为0，则是单队列，否则为多队列。 比如有4个文件夹，分别是 0,1,2,3.那么 就是4个队列。</p><h3 id="其他" tabindex="-1">其他 <a class="header-anchor" href="#其他" aria-label="Permalink to &quot;其他&quot;">​</a></h3><p>iostat里的util只能说明磁盘繁忙度，单看 100%并不一定有瓶颈。还要继续看await, await == IO在内核里排队调度 + IO在存储设备处理时间。是准 确值。 在单队列的磁盘上面， svctm可以看做io在底层处理时间。但如果是多队列，值不准。<br> 可以使用blktrace分析IO在每个阶段的准确耗时。<br> oracle物理机场景，发现磁盘调度改为none性能会更好。<br> 磁盘有预读功能， 可使用 /sys/block/sda/queue/read_ahead_kb 查询当前值</p><h2 id="文件系统" tabindex="-1">文件系统 <a class="header-anchor" href="#文件系统" aria-label="Permalink to &quot;文件系统&quot;">​</a></h2><p>ext4 如果在数据T级时发现性能慢，可用perf分析耗时高的函数</p><p>nfs 一个挂载点对应一个TCP连接。架构限制，没有太多优化手段</p><h2 id="容器" tabindex="-1">容器 <a class="header-anchor" href="#容器" aria-label="Permalink to &quot;容器&quot;">​</a></h2><p>影响POD内进程性能的常见因素：</p><ol><li>POD CPU 限流， 可通过busrt特性缓解</li><li>CPU绑核， 可通过kubelet配置， 设置绑核其实就是亲和性， 是否生效可通过<code>/proc/&lt;pid&gt;/status</code>里的Cpus_allowed_list确认</li><li>应用cgroup感知 容器里进程查询到的cpu/memory信息是node的，不是spec resource里的request,limit值。这样误以为资源很多，其实受cgroup限制。经常启动与node 节点cpu数据相同的线程数，但经常收到cgroup limit的限制。 发生限流或者内存较高的情况。<br> 高版本的java,golang已实现cgroup感知。</li></ol>`,68),s=[l];function i(p,c,n,h,d,u){return r(),a("div",null,s)}const m=e(o,[["render",i]]);export{f as __pageData,m as default};
